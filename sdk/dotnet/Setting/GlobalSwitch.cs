// *** WARNING: this file was generated by pulumi-language-dotnet. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

using System;
using System.Collections.Generic;
using System.Collections.Immutable;
using System.Threading.Tasks;
using Pulumi.Serialization;
using Pulumi;

namespace Pulumiverse.Unifi.Setting
{
    /// <summary>
    /// The `unifi.setting.GlobalSwitch` resource manages the switch isolation settings (device isolation and ACL-based layer-3 isolation) for a UniFi site, exposed in the controller UI under **Settings → Network → Switch Isolation Settings**.
    /// 
    /// This resource is intentionally narrow: it manages only the isolation-related fields of the controller's `GlobalSwitch` setting object. All other fields of that object (such as DHCP snooping, 802.1X, STP, jumbo frames, and flow control) are preserved untouched using a read-modify-write write path, so this resource can be adopted without clobbering settings managed elsewhere (for example, DHCP snooping via `unifi.setting.Usw`).
    /// 
    /// &gt; **Requires controller version 7.2 or later.** The Switch Isolation Settings are only available on UniFi Network controllers from version 7.2 onward.
    /// 
    /// &gt; **Clearing collections is not supported.** Because the underlying controller fields are `Omitempty`, setting any of `AclDeviceIsolation`, `AclL3Isolation`, or `SwitchExclusions` to an empty value cannot reliably clear it via the API. Configure at least one element. Removing theattribute does not unmanage or clear it: the last applied value is retained and kept in sync. Empty values are rejected at plan time.
    /// 
    /// ## Example Usage
    /// 
    /// ```csharp
    /// using System.Collections.Generic;
    /// using System.Linq;
    /// using Pulumi;
    /// using Unifi = Pulumiverse.Unifi;
    /// 
    /// return await Deployment.RunAsync(() =&gt; 
    /// {
    ///     // Define the networks used by the layer-3 isolation rules. The isolation
    ///     // settings reference networks by their UniFi network ID (the `id` attribute),
    ///     // not by name or CIDR.
    ///     var engineering = new Unifi.Network("engineering", new()
    ///     {
    ///         Name = "Engineering",
    ///         Purpose = "corporate",
    ///         Subnet = "10.0.10.1/24",
    ///         VlanId = 10,
    ///     });
    /// 
    ///     var guest = new Unifi.Network("guest", new()
    ///     {
    ///         Name = "Guest",
    ///         Purpose = "corporate",
    ///         Subnet = "10.0.20.1/24",
    ///         VlanId = 20,
    ///     });
    /// 
    ///     // Manage the site's switch isolation settings
    ///     // (Settings -&gt; Network -&gt; Switch Isolation Settings).
    ///     //
    ///     // Only the isolation-related fields are managed; all other global switch
    ///     // settings (DHCP snooping, STP, jumbo frames, etc.) are preserved untouched.
    ///     var example = new Unifi.Setting.GlobalSwitch("example", new()
    ///     {
    ///         AclL3Isolations = new[]
    ///         {
    ///             new Unifi.Setting.Inputs.GlobalSwitchAclL3IsolationArgs
    ///             {
    ///                 SourceNetwork = guest.Id,
    ///                 DestinationNetworks = new[]
    ///                 {
    ///                     engineering.Id,
    ///                 },
    ///             },
    ///         },
    ///         SwitchExclusions = new[]
    ///         {
    ///             "00:11:22:33:44:55",
    ///         },
    ///     });
    /// 
    /// });
    /// ```
    /// </summary>
    [UnifiResourceType("unifi:setting/globalSwitch:GlobalSwitch")]
    public partial class GlobalSwitch : global::Pulumi.CustomResource
    {
        /// <summary>
        /// Set of device identifiers to isolate (the controller's **Device Isolation** control). Each element is sent to the controller verbatim, with no validation or normalization: the UniFi `GlobalSwitch` API does not constrain this field's format, so supply the identifiers exactly as the controller expects them (refer to the controller UI). Reordering has no effect (this is an unordered set). At least one element is required when set; the value cannot be cleared and is retained even if the attribute is later removed.
        /// </summary>
        [Output("aclDeviceIsolations")]
        public Output<ImmutableArray<string>> AclDeviceIsolations { get; private set; } = null!;

        /// <summary>
        /// Set of layer-3 (network-to-network) isolation rules. Each entry isolates a source network from a set of destination networks. All values are UniFi network IDs (the `Id` of a `unifi.Network` resource), not network names or CIDRs. Reordering has no effect (unordered set).
        /// </summary>
        [Output("aclL3Isolations")]
        public Output<ImmutableArray<Outputs.GlobalSwitchAclL3Isolation>> AclL3Isolations { get; private set; } = null!;

        /// <summary>
        /// The name of the UniFi site where this resource should be applied. If not specified, the default site will be used.
        /// </summary>
        [Output("site")]
        public Output<string> Site { get; private set; } = null!;

        /// <summary>
        /// Set of switch MAC addresses excluded from isolation enforcement. Each element must be the MAC address of a switch that is already adopted/managed by the controller; the controller rejects MACs that do not correspond to a known switch. MAC addresses are case-insensitive and may use `:` or `-` separators (e.g. `aa:bb:cc:dd:ee:ff` and `AA-BB-CC-DD-EE-FF` are treated as the same address and produce no diff); the value is kept as written. At least one element is required when set; the value cannot be cleared and is retained even if the attribute is later removed.
        /// </summary>
        [Output("switchExclusions")]
        public Output<ImmutableArray<string>> SwitchExclusions { get; private set; } = null!;


        /// <summary>
        /// Create a GlobalSwitch resource with the given unique name, arguments, and options.
        /// </summary>
        ///
        /// <param name="name">The unique name of the resource</param>
        /// <param name="args">The arguments used to populate this resource's properties</param>
        /// <param name="options">A bag of options that control this resource's behavior</param>
        public GlobalSwitch(string name, GlobalSwitchArgs? args = null, CustomResourceOptions? options = null)
            : base("unifi:setting/globalSwitch:GlobalSwitch", name, args ?? new GlobalSwitchArgs(), MakeResourceOptions(options, ""))
        {
        }

        private GlobalSwitch(string name, Input<string> id, GlobalSwitchState? state = null, CustomResourceOptions? options = null)
            : base("unifi:setting/globalSwitch:GlobalSwitch", name, state, MakeResourceOptions(options, id))
        {
        }

        private static CustomResourceOptions MakeResourceOptions(CustomResourceOptions? options, Input<string>? id)
        {
            var defaultOptions = new CustomResourceOptions
            {
                Version = Utilities.Version,
                PluginDownloadURL = "github://api.github.com/pulumiverse",
            };
            var merged = CustomResourceOptions.Merge(defaultOptions, options);
            // Override the ID if one was specified for consistency with other language SDKs.
            merged.Id = id ?? merged.Id;
            return merged;
        }
        /// <summary>
        /// Get an existing GlobalSwitch resource's state with the given name, ID, and optional extra
        /// properties used to qualify the lookup.
        /// </summary>
        ///
        /// <param name="name">The unique name of the resulting resource.</param>
        /// <param name="id">The unique provider ID of the resource to lookup.</param>
        /// <param name="state">Any extra arguments used during the lookup.</param>
        /// <param name="options">A bag of options that control this resource's behavior</param>
        public static GlobalSwitch Get(string name, Input<string> id, GlobalSwitchState? state = null, CustomResourceOptions? options = null)
        {
            return new GlobalSwitch(name, id, state, options);
        }
    }

    public sealed class GlobalSwitchArgs : global::Pulumi.ResourceArgs
    {
        [Input("aclDeviceIsolations")]
        private InputList<string>? _aclDeviceIsolations;

        /// <summary>
        /// Set of device identifiers to isolate (the controller's **Device Isolation** control). Each element is sent to the controller verbatim, with no validation or normalization: the UniFi `GlobalSwitch` API does not constrain this field's format, so supply the identifiers exactly as the controller expects them (refer to the controller UI). Reordering has no effect (this is an unordered set). At least one element is required when set; the value cannot be cleared and is retained even if the attribute is later removed.
        /// </summary>
        public InputList<string> AclDeviceIsolations
        {
            get => _aclDeviceIsolations ?? (_aclDeviceIsolations = new InputList<string>());
            set => _aclDeviceIsolations = value;
        }

        [Input("aclL3Isolations")]
        private InputList<Inputs.GlobalSwitchAclL3IsolationArgs>? _aclL3Isolations;

        /// <summary>
        /// Set of layer-3 (network-to-network) isolation rules. Each entry isolates a source network from a set of destination networks. All values are UniFi network IDs (the `Id` of a `unifi.Network` resource), not network names or CIDRs. Reordering has no effect (unordered set).
        /// </summary>
        public InputList<Inputs.GlobalSwitchAclL3IsolationArgs> AclL3Isolations
        {
            get => _aclL3Isolations ?? (_aclL3Isolations = new InputList<Inputs.GlobalSwitchAclL3IsolationArgs>());
            set => _aclL3Isolations = value;
        }

        /// <summary>
        /// The name of the UniFi site where this resource should be applied. If not specified, the default site will be used.
        /// </summary>
        [Input("site")]
        public Input<string>? Site { get; set; }

        [Input("switchExclusions")]
        private InputList<string>? _switchExclusions;

        /// <summary>
        /// Set of switch MAC addresses excluded from isolation enforcement. Each element must be the MAC address of a switch that is already adopted/managed by the controller; the controller rejects MACs that do not correspond to a known switch. MAC addresses are case-insensitive and may use `:` or `-` separators (e.g. `aa:bb:cc:dd:ee:ff` and `AA-BB-CC-DD-EE-FF` are treated as the same address and produce no diff); the value is kept as written. At least one element is required when set; the value cannot be cleared and is retained even if the attribute is later removed.
        /// </summary>
        public InputList<string> SwitchExclusions
        {
            get => _switchExclusions ?? (_switchExclusions = new InputList<string>());
            set => _switchExclusions = value;
        }

        public GlobalSwitchArgs()
        {
        }
        public static new GlobalSwitchArgs Empty => new GlobalSwitchArgs();
    }

    public sealed class GlobalSwitchState : global::Pulumi.ResourceArgs
    {
        [Input("aclDeviceIsolations")]
        private InputList<string>? _aclDeviceIsolations;

        /// <summary>
        /// Set of device identifiers to isolate (the controller's **Device Isolation** control). Each element is sent to the controller verbatim, with no validation or normalization: the UniFi `GlobalSwitch` API does not constrain this field's format, so supply the identifiers exactly as the controller expects them (refer to the controller UI). Reordering has no effect (this is an unordered set). At least one element is required when set; the value cannot be cleared and is retained even if the attribute is later removed.
        /// </summary>
        public InputList<string> AclDeviceIsolations
        {
            get => _aclDeviceIsolations ?? (_aclDeviceIsolations = new InputList<string>());
            set => _aclDeviceIsolations = value;
        }

        [Input("aclL3Isolations")]
        private InputList<Inputs.GlobalSwitchAclL3IsolationGetArgs>? _aclL3Isolations;

        /// <summary>
        /// Set of layer-3 (network-to-network) isolation rules. Each entry isolates a source network from a set of destination networks. All values are UniFi network IDs (the `Id` of a `unifi.Network` resource), not network names or CIDRs. Reordering has no effect (unordered set).
        /// </summary>
        public InputList<Inputs.GlobalSwitchAclL3IsolationGetArgs> AclL3Isolations
        {
            get => _aclL3Isolations ?? (_aclL3Isolations = new InputList<Inputs.GlobalSwitchAclL3IsolationGetArgs>());
            set => _aclL3Isolations = value;
        }

        /// <summary>
        /// The name of the UniFi site where this resource should be applied. If not specified, the default site will be used.
        /// </summary>
        [Input("site")]
        public Input<string>? Site { get; set; }

        [Input("switchExclusions")]
        private InputList<string>? _switchExclusions;

        /// <summary>
        /// Set of switch MAC addresses excluded from isolation enforcement. Each element must be the MAC address of a switch that is already adopted/managed by the controller; the controller rejects MACs that do not correspond to a known switch. MAC addresses are case-insensitive and may use `:` or `-` separators (e.g. `aa:bb:cc:dd:ee:ff` and `AA-BB-CC-DD-EE-FF` are treated as the same address and produce no diff); the value is kept as written. At least one element is required when set; the value cannot be cleared and is retained even if the attribute is later removed.
        /// </summary>
        public InputList<string> SwitchExclusions
        {
            get => _switchExclusions ?? (_switchExclusions = new InputList<string>());
            set => _switchExclusions = value;
        }

        public GlobalSwitchState()
        {
        }
        public static new GlobalSwitchState Empty => new GlobalSwitchState();
    }
}

// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages UniFi Switch (USW) settings for a UniFi site. These settings control global switch behaviors such as DHCP snooping.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as unifi from "@pulumiverse/unifi";
 *
 * const example = new unifi.setting.Usw("example", {dhcpSnoop: true});
 * ```
 */
export class Usw extends pulumi.CustomResource {
    /**
     * Get an existing Usw resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: UswState, opts?: pulumi.CustomResourceOptions): Usw {
        return new Usw(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'unifi:setting/usw:Usw';

    /**
     * Returns true if the given object is an instance of Usw.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Usw {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Usw.__pulumiType;
    }

    /**
     * Whether DHCP snooping is enabled. DHCP snooping is a security feature that filters untrusted DHCP messages and builds a binding database of valid hosts.
     */
    declare public readonly dhcpSnoop: pulumi.Output<boolean>;
    /**
     * The name of the UniFi site where this resource should be applied. If not specified, the default site will be used.
     */
    declare public readonly site: pulumi.Output<string>;

    /**
     * Create a Usw resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: UswArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: UswArgs | UswState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as UswState | undefined;
            resourceInputs["dhcpSnoop"] = state?.dhcpSnoop;
            resourceInputs["site"] = state?.site;
        } else {
            const args = argsOrState as UswArgs | undefined;
            if (args?.dhcpSnoop === undefined && !opts.urn) {
                throw new Error("Missing required property 'dhcpSnoop'");
            }
            resourceInputs["dhcpSnoop"] = args?.dhcpSnoop;
            resourceInputs["site"] = args?.site;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Usw.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Usw resources.
 */
export interface UswState {
    /**
     * Whether DHCP snooping is enabled. DHCP snooping is a security feature that filters untrusted DHCP messages and builds a binding database of valid hosts.
     */
    dhcpSnoop?: pulumi.Input<boolean>;
    /**
     * The name of the UniFi site where this resource should be applied. If not specified, the default site will be used.
     */
    site?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a Usw resource.
 */
export interface UswArgs {
    /**
     * Whether DHCP snooping is enabled. DHCP snooping is a security feature that filters untrusted DHCP messages and builds a binding database of valid hosts.
     */
    dhcpSnoop: pulumi.Input<boolean>;
    /**
     * The name of the UniFi site where this resource should be applied. If not specified, the default site will be used.
     */
    site?: pulumi.Input<string>;
}

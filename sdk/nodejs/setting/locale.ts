// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages locale settings for a UniFi site.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as unifi from "@pulumiverse/unifi";
 *
 * const example = new unifi.setting.Locale("example", {timezone: "America/New_York"});
 * ```
 */
export class Locale extends pulumi.CustomResource {
    /**
     * Get an existing Locale resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: LocaleState, opts?: pulumi.CustomResourceOptions): Locale {
        return new Locale(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'unifi:setting/locale:Locale';

    /**
     * Returns true if the given object is an instance of Locale.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Locale {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Locale.__pulumiType;
    }

    /**
     * The name of the UniFi site where this resource should be applied. If not specified, the default site will be used.
     */
    declare public readonly site: pulumi.Output<string>;
    /**
     * Timezone for the UniFi controller, e.g., `America/Los_Angeles`
     */
    declare public readonly timezone: pulumi.Output<string>;

    /**
     * Create a Locale resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: LocaleArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: LocaleArgs | LocaleState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as LocaleState | undefined;
            resourceInputs["site"] = state?.site;
            resourceInputs["timezone"] = state?.timezone;
        } else {
            const args = argsOrState as LocaleArgs | undefined;
            if (args?.timezone === undefined && !opts.urn) {
                throw new Error("Missing required property 'timezone'");
            }
            resourceInputs["site"] = args?.site;
            resourceInputs["timezone"] = args?.timezone;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Locale.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Locale resources.
 */
export interface LocaleState {
    /**
     * The name of the UniFi site where this resource should be applied. If not specified, the default site will be used.
     */
    site?: pulumi.Input<string>;
    /**
     * Timezone for the UniFi controller, e.g., `America/Los_Angeles`
     */
    timezone?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a Locale resource.
 */
export interface LocaleArgs {
    /**
     * The name of the UniFi site where this resource should be applied. If not specified, the default site will be used.
     */
    site?: pulumi.Input<string>;
    /**
     * Timezone for the UniFi controller, e.g., `America/Los_Angeles`
     */
    timezone: pulumi.Input<string>;
}
